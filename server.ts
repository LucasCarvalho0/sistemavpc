// server.ts
//
// Este arquivo foi mantido apenas como referência histórica.
// O sistema agora usa Server-Sent Events (SSE) via /api/events
// em vez de WebSocket, eliminando a necessidade de servidor customizado.
//
// Para desenvolvimento: npm run dev (usa Next.js dev server padrão)
// Para produção: npm run build && npm start
